const myObject = { key1: 'joel', key2: 'odiyan', key3: 42 };

// Convert the JavaScript object to a formatted JSON string with 2-space indentation
const jsonString = JSON.stringify(myObject, null, 2);

console.log(jsonString);
